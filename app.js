// Roadora clean app.js
// Alle Mijn Roadtrip specifieke panels, hero en listeners verwijderd
document.addEventListener('DOMContentLoaded', () => {
  // Leeg format ready
  const container = document.getElementById('roadtripContainer');
  container.innerHTML = ''; // Helemaal leeg
});